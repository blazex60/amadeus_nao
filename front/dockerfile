FROM node:20-slim
WORKDIR /app

# 依存関係ファイルをコピー (ビルドコンテキストはプロジェクトルートなので front/ を付ける)
COPY front/package.json front/package-lock.json* ./

# 依存関係をインストール
RUN npm install

# Viteのデフォルトポート 5173 を公開
EXPOSE 5173
# 開発モードで起動
CMD ["npm", "run", "dev", "--", "--host"]